export default function ProductRevealCopy() {
  return (
    <div className="product-reveal-copy" aria-labelledby="product-reveal-title">
      <p className="home-eyebrow">CAFECANOPY PRODUCT</p>
      <h2 id="product-reveal-title">The whole café, in one view.</h2>
      <p>From the first order to the final report, every team stays in sync.</p>
    </div>
  );
}
